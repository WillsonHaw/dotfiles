*.log

# Secrets — never commit
*.key
*.pem
*.kdbx
*.gpg
*.asc
id_rsa*
id_ed25519*
id_ecdsa*
.env
.env.*
config.local
*.local

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
